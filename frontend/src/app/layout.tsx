import type { Metadata } from 'next';
import './globals.css';
import { ThemeProvider } from '@/components/ThemeProvider';

export const metadata: Metadata = {
  title: 'Streamlytics AI | Netflix Intelligence Hub',
  description:
    'AI-powered personalised Netflix recommendations with cluster-based analysis, churn prediction, and smart content discovery.',
  keywords: ['netflix', 'recommendations', 'AI', 'machine learning', 'streamlytics'],
  authors: [{ name: 'Sujal Prajapati' }],
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" suppressHydrationWarning>
      <head>
        <link
          rel="stylesheet"
          href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
        />
      </head>
      <body className="antialiased" suppressHydrationWarning>
        <ThemeProvider>{children}</ThemeProvider>
      </body>
    </html>
  );
}
